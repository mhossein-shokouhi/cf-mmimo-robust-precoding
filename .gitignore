# Python virtual environments
.venv/
venv/
env/

# Bytecode / caches
__pycache__/
*.pyc
*.pyo
*.pyd

# OS metadata
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# LaTeX build artefacts (the paper source itself is committed)
*.aux
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.log
*.out
*.synctex.gz
*.toc

# Local / experimental
*.tmp
.scratch/

# Trained DRL agent checkpoints — large (111 MB+) and trivially
# regenerable from the code on first sweep run; they are cached under
# `models/naive_K{K}_taup{tau_p}_L{L}_seed{seed}.npz` per operating
# point. Keep `proposed.npz` in case it is ever shipped explicitly.
models/naive_*.npz

# Local-only LaTeX source (do not publish)
ORAN.tex
